






//PsuedoCode

// require express . router to start the routing for the api


// const router = require('express').Router();
// const {
//   getStudents,
//   getSingleStudent,
//   createStudent,
//   deleteStudent,
//   addAssignment,
//   removeAssignment,
// } = require('../../controllers/studentController');

// Then the Crud methods that are defined in the COntroller is required here

// Route the Api user to the get User(userfile).post(create user)
//          - route the APi user on some other post methods

//            // /api/students
//           router.route('/').get(getStudents).post(createStudent);

//                // /api/students/:studentId
//                router.route('/:studentId').get(getSingleStudent).delete(deleteStudent);

//              // /api/students/:studentId/assignments
//                   router.route('/:studentId/assignments').post(addAssignment);

//                      // /api/students/:studentId/assignments/:assignmentId
//                   router.route('/:studentId/assignments/:assignmentId').delete(removeAssignment);

// export it all
//              module.exports = router;





